{
  "name": "data-dashboard-agent",
  "version": "0.1.0",
  "private": true,
  "description": "데이터 대시보드 AgentHub 에이전트 — CSV/JSON → KPI·차트 (Agent Function + Surface)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "validate": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test tests/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "//": "@agenthub/sdk and @agenthub/surface-sdk are provided by the AgentHub build/runtime (see types/*.d.ts ambient stubs); they are not installed from a registry.",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "tsx": "^4.19.2",
    "typescript": "~5.6.0"
  }
}
