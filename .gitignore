node_modules/
*.log
.DS_Store
.env
coverage/

# dist/ is intentionally tracked: the AgentHub Build Pipeline (§5.3) trusts a
# prebuilt dist/ and skips npm. `npm run build` regenerates it from source.
